// Loading spinner component
// Displays an animated spinning loader for async operations
const Loader = () => {
    return (
        <div className="animate-spin rounded-full h-5 w-5 border-b-2 border-white"></div>
    )
}

export default Loader;